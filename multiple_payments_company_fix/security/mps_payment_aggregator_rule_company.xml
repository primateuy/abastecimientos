<odoo>
    <!-- Record rule to allow access across all companies enabled for the user -->
    <record id="mps_payment_aggregator_rule_company_fix" model="ir.rule">
        <field name="name">Payment aggregator by company (fix)</field>
        <field name="model_id" ref="multiple_payments.model_mps_payment_aggregator"/>
        <field name="domain_force">[('company_id','in', user.company_ids.ids)]</field>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>
</odoo>